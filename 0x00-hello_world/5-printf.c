#include <stdio.h>

/**
 * main - A main function to printf
 * Discription: A C programme to display a statement
 * Return: Always return 0
*/

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
